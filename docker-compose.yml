services:
  web:
    build: .
    ports:
      - "8889:80"
    volumes:
      - ./src:/var/www/html
    networks:
      - app-network

networks:
  app-network:
    name: feedback_rstn_app-network
    external: true
