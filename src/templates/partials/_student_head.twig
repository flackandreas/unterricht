{#
    Gemeinsamer Kopf der Schülerseiten.
    Vorher war der komplette Block in student_homework.twig und
    student_homework_view.twig dupliziert - 479 identische Zeilen, weshalb
    Korrekturen regelmäßig nur in einer der beiden Dateien landeten.
#}
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="{{ asset('/css/fonts.css') }}" rel="stylesheet">
<script src="{{ asset('/vendor/sweetalert2/sweetalert2.all.min.js') }}"></script>
<script src="{{ asset('/vendor/canvas-confetti/confetti.browser.min.js') }}"></script>
<script src="{{ asset('/js/avatar.js') }}"></script>
<style>
    :root {
        --primary-color: #2ecc71;
        --primary-dark: #27ae60;
        --bg-color: #f8fafc;
        --text-dark: #2c3e50;
        --border-color: #e2e8f0;
        --danger-color: #e74c3c;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background-color: var(--bg-color);
        color: var(--text-dark);
        margin: 0;
        padding: 20px;
        display: flex;
        justify-content: center;
        min-height: 100vh;
    }
    .container {
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        padding: 40px;
        width: 100%;
        max-width: 640px;
        margin-top: 40px;
        align-self: flex-start;
    }
    h1 { margin-top: 0; color: var(--primary-dark); font-size: 1.5rem; }
    .meta { color: #64748b; font-size: 0.9rem; margin-bottom: 20px; padding-bottom: 20px; border-bottom: 1px solid var(--border-color); }
    .form-group { margin-bottom: 20px; }
    label { display: block; font-weight: 500; margin-bottom: 8px; }
    input[type="text"], input[type="file"] {
        width: 100%;
        padding: 12px;
        border: 1px solid var(--border-color);
        border-radius: 6px;
        box-sizing: border-box;
        font-family: inherit;
    }
    input[type="file"] { padding: 10px; background: #f8fafc; cursor: pointer; }
    button {
        width: 100%;
        padding: 14px;
        background-color: var(--primary-color);
        color: white;
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background-color 0.2s;
    }
    button:hover { background-color: var(--primary-dark); }
    .alert { padding: 15px; border-radius: 6px; margin-bottom: 20px; }
    .alert-error { background-color: #fee2e2; color: #991b1b; border: 1px solid #f87171; }
    .alert-success { background-color: #dcfce7; color: #166534; border: 1px solid #86efac; }
    .alert-info { background-color: #e0f2fe; color: #075985; border: 1px solid #7dd3fc; }

    /* Fehler-Overlay über dem Foto */
    .image-overlay-container { position: relative; display: inline-block; width: 100%; }
    .error-box {
        position: absolute;
        border: 2px solid var(--danger-color);
        background: rgba(231, 76, 60, 0.15);
        cursor: pointer;
        transition: all 0.2s ease-in-out;
        border-radius: 6px;
    }
    .error-box.highlight, .error-box:hover {
        background: rgba(231, 76, 60, 0.35);
        border-color: #c0392b;
        box-shadow: 0 0 12px rgba(231, 76, 60, 0.6);
        z-index: 10;
    }
    .error-pin {
        position: absolute;
        top: -12px;
        left: -12px;
        background: var(--danger-color);
        color: white;
        font-weight: 700;
        font-size: 0.8rem;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 2px 6px rgba(0,0,0,0.3);
        border: 2px solid white;
        z-index: 11;
    }
    .error-box::after {
        content: attr(data-description);
        position: absolute;
        bottom: 105%;
        left: 50%;
        transform: translateX(-50%);
        background: #2c3e50;
        color: white;
        padding: 8px 12px;
        border-radius: 6px;
        font-size: 0.85rem;
        line-height: 1.4;
        white-space: pre-wrap;
        width: max-content;
        max-width: 250px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.2s, visibility 0.2s;
        pointer-events: none;
        z-index: 100;
        font-weight: normal;
        text-align: left;
    }
    .error-box:hover::after { opacity: 1; visibility: visible; }
    .error-list-item {
        background: #fff;
        border: 1px solid var(--border-color);
        border-left: 4px solid var(--danger-color);
        border-radius: 8px;
        padding: 12px 16px;
        margin-bottom: 10px;
        transition: all 0.2s ease-in-out;
        cursor: pointer;
    }
    .error-list-item:hover, .error-list-item.active {
        background: #fef2f2;
        border-color: #fca5a5;
        border-left-color: #dc2626;
        transform: translateX(3px);
    }

    .loading { text-align: center; margin-top: 20px; }
    .spinner {
        border: 4px solid rgba(0, 0, 0, 0.1);
        width: 36px;
        height: 36px;
        border-radius: 50%;
        border-left-color: var(--primary-color);
        animation: spin 1s linear infinite;
        margin: 0 auto 15px auto;
    }
    @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
</style>
